network:
  interface: null  # null = auto-select active adapter
  time_window: 10.0
  syn_threshold: 20

logging:
  level: "DEBUG"
  log_to_file: true

database:
  enabled: true
  path: "logs/attacks.db"